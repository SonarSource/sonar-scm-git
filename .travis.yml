language: java
sudo: false
install: true

jdk:
  - oraclejdk7

script:
  - ./travis.sh

env:
  matrix:
    - TARGET=CI
    - TARGET=IT SQ_VERSION=5.1
    - TARGET=IT SQ_VERSION=DEV

cache:
  directories:
    - $HOME/.m2/repository
    # for integration tests and analysis
    - $HOME/.sonar

notifications:
  email: false
